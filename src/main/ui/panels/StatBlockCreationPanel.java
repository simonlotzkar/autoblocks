package ui.panels;

import java.awt.*;

// Represents...
public class StatBlockCreationPanel extends MenuPanel {
    // constructs this panel
    public StatBlockCreationPanel(MenuCardPanel menuCardPanel) {
        super(new BorderLayout(), menuCardPanel);
    }
}
