package ui.panels;

import ui.panels.menus.MenuManagerPanel;
import ui.panels.menus.MenuPanel;

import java.awt.*;

// Represents...
public class StatBlockCreationPanel extends MenuPanel {
    // constructs this panel
    public StatBlockCreationPanel(MenuManagerPanel menuManagerPanel) {
        super(new BorderLayout(), menuManagerPanel);
    }
}
